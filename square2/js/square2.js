$(document).ready(function(){
	
	/*// Attach a submit handler to the form
	$( "#book_form" ).submit(function( event ) {
		
		// Stop form from submitting normally
		event.preventDefault();
		console.log("go form");
		$.post( "data/form_submit.php" );
	});
*/
});
 