var squareApp = angular.module('squareApp', ['ngRoute','bookControllers']);

/*squareApp.config(['$routeProvider', function($routeProvider) {
  $routeProvider.
  when('/list', {
    templateUrl: 'partials/list.php',
//    templateUrl: 'partials/list.php',
    controller: 'EmpListController'
  }).
  otherwise({
    redirectTo: '/list'
  });
}]);

*/
